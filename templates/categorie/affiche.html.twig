{% extends 'base.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block body %}
<div class="container">
    <h1>{{cat.titre}}</h1>
    
    <table class="table table-dark table-striped">
      <thead>
        <tr>
          <th>Id</th>
          <th>Titre</th>
          <th>Resume</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td> {{cat.id}}</td>
          <td> {{cat.titre}}</td>
          <td> {{cat.resume}}</td>
        </tr>
      </tbody>
    </table>
  </div>

{% endblock %}
